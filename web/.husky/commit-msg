# =============================================================================
# Husky commit-msg 钩子 —— 提交信息规范门禁（Conventional Commits）
# -----------------------------------------------------------------------------
# 触发时机：`git commit` 生成提交信息后、真正创建提交前由 git 自动执行。
# 执行内容：commitlint 读取提交信息文件（$1 为 git 传入的临时文件路径），
#           按 web/commitlint.config.js 的规则校验格式。
# 失败行为：提交信息不符合 Conventional Commits 规范时中断提交。
#
# 规范示例：feat(web): 新增主题切换  /  fix: 修复分页错误
# =============================================================================
cd web
pnpm exec commitlint --edit "$1"
