# =============================================================================
# Husky pre-commit 钩子 —— 提交前代码质量门禁
# -----------------------------------------------------------------------------
# 触发时机：每次 `git commit` 之前由 git 自动执行。
# 执行内容：对「本次暂存（staged）」的前端文件运行 lint-staged，
#           即 eslint --fix（见 web/package.json 的 lint-staged 配置）。
# 失败行为：任一命令非零退出则中断本次提交，问题文件不会被提交。
#
# 注意：git 钩子的工作目录为仓库根（secretpad/），而 pnpm workspace 位于
#       web/ 子目录，因此需先 cd web 再执行 pnpm 命令。
# =============================================================================
cd web
pnpm lint-staged
