ignored-builds=[]
only-built-dependencies=["esbuild"]
# 使用 npmmirror 镜像源，避免在中国大陆网络下访问 npmjs.org 时频繁出现
# ECONNRESET / 超时，导致 pnpm install 无法完成。与旧 frontend-src 的 .npmrc
# 保持一致，便于 CI 和本地开发复用缓存。
registry=https://registry.npmmirror.com
